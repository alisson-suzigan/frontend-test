const
  // Endpoint paths
  CHAT_HISTORY_PATH = '',

  // Window status
  WINDOW = {
    OPENED: 'OPENED',
    MINIMIZED: 'MINIMIZED',
    CLOSED: 'CLOSED'
  };


export {
  CHAT_HISTORY_PATH,
  WINDOW
};