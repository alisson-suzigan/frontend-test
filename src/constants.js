const
  // Endpoint paths
  CHAT_HISTORY_PATH = 'http://localhost:3000/json/talk.json',

  // Window status
  WINDOW = {
    OPENED: 'OPENED',
    MINIMIZED: 'MINIMIZED',
    CLOSED: 'CLOSED'
  };


export {
  CHAT_HISTORY_PATH,
  WINDOW
};